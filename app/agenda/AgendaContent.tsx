import { season2026 } from "@/lib/agenda";

export default function AgendaContent() {
  const events = season2026;
  return <><section className="page-hero"><div className="page-hero-grid" aria-hidden="true" /><p className="section-index">03 — Agenda</p><div><p className="eyebrow"><span /> Temporada 2026</p><h1>Próximas<br /><em>notas.</em></h1></div><p className="page-intro">Conciertos del Ciclo, presentaciones especiales y novedades de nuestra actividad coral.</p></section><section className="agenda-page"><div className="announcement-list">{events.map((item,index) => <article id={`evento-${Math.abs(item.id)}`} key={item.id}><span>{String(index+1).padStart(2,"0")}</span><div className="announcement-copy"><small>{item.eventDate ? new Date(item.eventDate+"T12:00:00").toLocaleDateString("es-AR",{weekday:"long",day:"2-digit",month:"long",year:"numeric"}) : "Novedad"}</small><h2>{item.title}</h2><p>{item.summary}</p>{item.participants && <div className="announcement-guests"><small>Coros invitados</small><ul>{item.participants.map(participant => <li key={participant.name}><strong>{participant.name}</strong><span>Dir. {participant.director}</span></li>)}</ul></div>}</div><div className="announcement-place">{item.time && <strong>{item.time}</strong>}{item.mapUrl ? <a href={item.mapUrl} target="_blank" rel="noreferrer"><span>{item.location || "Ver ubicación"}</span><small>Ver en Maps</small></a> : item.location || "Coro FIUBA"}</div></article>)}</div><aside className="agenda-aside"><small>Ensayos regulares</small><strong>Todos los viernes<br />19:30 — 22:00 h</strong><a href="https://maps.app.goo.gl/p9ZfPAG2v7PEmbyP6" target="_blank" rel="noreferrer"><span>Paseo Colón 850</span><small>Ver en Maps</small></a><p>El Ciclo de Conciertos Corales y presentaciones como La Noche de los Museos tienen lugar en la sede Las Heras.</p><a href="https://maps.app.goo.gl/GXmzPA9sefcX2J5D7" target="_blank" rel="noreferrer"><span>Sede Las Heras</span><small>Ver en Maps</small></a></aside></section></>;
}
